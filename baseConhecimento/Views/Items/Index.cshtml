@model IEnumerable<baseConhecimento.Models.Item>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<meta name="viewport" content="width=device-width" />
<title>Base de Conhecimento - CVC</title>
<script src="~/Scripts/jquery-2.2.1.min.js"></script>  

<h2>Nossos Itens</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
    @{
        int i = 1;
    }

<div id="tabela">
    <div class="linha">
        
    @foreach (var itens in Model)
    {
        <a href="/Items/Details/@itens.id_ic">
            <div class="coluna" id="@i">
                    <img src="~/Imagens/@(itens.imagem).jpg" alt="Sem Imagem">
                <p class="nome"> @itens.nome </p>
                @*Html.ActionLink(itens.nome, "Details", new { id = itens.id_ic })*@
            </div>
            @*Html.ActionLink("Detalhes", "Details", new { id = itens.id_ic }) |*@
            <div>
            </div>
        </a>
        i = ++i;
    }
    </div>
</div>
<script>
   $(document).ready(function () {
        var num1 = 1;
        var num2 = @i;
       /* for(num1; num1 < num2; num1++)
        {
            $('tr').click(function(){
                $('td', this).click();
            });
        }*/
   });
</script>
